## step 1

考え方は、今のノードと次のノードを見比べて、重複していたら削除するというものです。  
削除の書き方が分からず、delete や append などを試しに書いてみました。  
```Python
# 最初に自分で書いた誤ったコード
class Solution:
    def deleteDuplicates(self, head: Optional[ListNode]) -> Optional[ListNode]:
        visited = set()
        now_node = head
        next_node = head
        while now_node is not None:
            if now_node in visited:
                return now_node
            visited.add(now_node)
            now_node = now_node.next
            next_node = now_node.next.next
            if now_node == next_node in visited:
                visited.append(now_node)
                return next_node
```
他の人のコードを参考にしつつ、改めて問題を読み返したところ、この問題は値を求めるものだと分かりました。   

https://github.com/oto10/leetcode-arai60/pull/3/changes  

https://github.com/fuga-98/arai60/pull/4/changes  

```Python
# 他人の解答を参考にして、自分のコードを修正した
class Solution:
    def deleteDuplicates(self, head: Optional[ListNode]) -> Optional[ListNode]:
        now_node = head
        while now_node is not None and now_node.next:
            if now_node.val == now_node.next.val:
                now_node.next = now_node.next.next
            else:
                now_node = now_node.next
        return head
```
