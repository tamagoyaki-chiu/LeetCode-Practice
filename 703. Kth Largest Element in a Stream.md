# 進め方

Step1 : 問題を解く。

Step2 : 他の人のPRを参照する。

Step3 : 3回続けてエラーが出ないように書く。ドキュメントを参照する。

## step 1

まず、自分で考えたコードを書いてみた。  
しかし、クラスの扱い方や問題の意図がよく分からず、正しく実装できなかった。  
また、降順ソートの書き方（sort(reverse=True)）を知らなかった。  
そのまま失敗しました。  
###  諦めたコード
```python
class KthLargest:

    def __init__(self, k: int, nums: List[int]):
        obj = KthLargest(k, nums)
        result_obj = 0
            sort.obj
            while k is not None:
                result_obj = obj[int]
            return result_obj

    def add(self, val: int) -> int:
        point_1 = obj.add(val)
            while k is not None:
                point_1 = obj.add(val)
                result_obj = point_1[int]
            return result_obj
```

## step 2

ChatGPT に自分のコードを渡して修正を依頼した　　
### 解きなおす
```python
class KthLargest:

    def __init__(self, k: int, nums: List[int]):
        self.k = k
        self.nums = nums

    def add(self, val: int) -> int:
        self.nums.append(val)
        self.nums.sort(reverse=True)
        return self.nums[self.k - 1]
```

### sort(reverse=True)  
```python
nums.sort(reverse=True)
```
`reverse=True` を指定すると、降順（大きい順）にソートされる。    
```python
例：
nums = [4, 5, 8, 2]
nums.sort(reverse=True)

# [8, 5, 4, 2]
```
指定しない場合は昇順（小さい順）になる。  
```python  
nums.sort()
# [2, 4, 5, 8]
```
---

### self.k - 1 の理由  

Python のリストは 0 始まりのインデックスを使用する。  
```python  
例： nums = [8, 5, 4, 2]
```
| 順位 | 値 | インデックス |
| -- | - | ------ |
| 1位 | 8 | 0      |
| 2位 | 5 | 1      |
| 3位 | 4 | 2      |
| 4位 | 2 | 3      |

そのため、k 番目に大きい要素を取得する場合は  
```python
nums[k - 1]
```
を使用する。  
```python  
例：
k = 3

nums[k - 1]
# nums[2]
# 4
```
`-1` は要素数を調整するためではなく、Python のインデックスが 0 から始まるためである。  

### 他の方の PR を見てみる  


refer to  
https://github.com/MA-yo-TA/leetcode/pull/9/changes  
https://github.com/wanwan87/LeetCode_arai60/pull/8/changes  
https://github.com/hiro111208/leetcode/pull/8/changes  

### `heapq.heappop()` の使い方を学んだ
```python
heapq.heappop(heap)
```
`heappop()` は Min Heap の最小要素を取り出し、同時に削除する。  

```python
例：
heap = [4, 5, 8]
heapq.heappush(heap, 2)

# heap = [2, 4, 8, 5]

heapq.heappop(heap)

# heap = [4, 5, 8]
```
Heap はソート済みのリストではないが、`heap[0]` には常に最小値が格納される。  
この問題では、Heap のサイズが k を超えた場合に `heappop()` を使って最小値を削除し、  
「最大の k 個の要素だけを保持する」という考え方を学んだ。  
```python
import heapq
from typing import List

class KthLargest:
    def __init__(self, k: int, nums: List[int]):
        self.k = k
        self.largest_k = []
        for num in nums:
            self.add(num)

    def add(self, val: int) -> int:
        heapq.heappush(self.largest_k, val)
        while len(self.largest_k) > self.k:
            heapq.heappop(self.largest_k)
        return self.largest_k[0]
```
### heapq.heappush()
heapq.heappush(heap, val) Heap に新しい要素を追加する。
```
例：
heap = [3, 5, 8]
heapq.heappush(heap, 2)

結果：
[2, 3, 8, 5]
```
Heap の性質を維持したまま要素が追加される。
```python
class KthLargest:

    def __init__(self, k: int, nums: List[int]):
        self.k = k
        self.top_k = nums[:k]
        heapq.heapify(self.top_k)
        for i in range(k,len(nums)):
            heapq.heappushpop(self.top_k,nums[i])

    def add(self, val: int) -> int:
        heapq.heappush(self.top_k,val)
        if len(self.top_k) > self.k:
            heapq.heappop(self.top_k)
        return self.top_k[0]
```
### heapq.heapify()
heapq.heapify(nums)リストを Min Heap に変換する。
```
例：
nums = [10, 3, 7, 5]
heapq.heapify(nums)
```
Heap はソート済みにはならないが、nums[0] が常に最小値になる。
用途：既存のリストを Heap として扱いたい場合に使用する。
```python
class KthLargest:

    def __init__(self, k: int, nums: List[int]):
        self.k = k
        self.kth_highest_scores = nums
        heapq.heapify(self.kth_highest_scores)
        while len(self.kth_highest_scores) > self.k:
            heapq.heappop(self.kth_highest_scores)

    def add(self, val: int) -> int:
        heapq.heappush(self.kth_highest_scores, val)
        while len(self.kth_highest_scores) > self.k:
            heapq.heappop(self.kth_highest_scores)
        return self.kth_highest_scores[0]
```

## step 3  
3回続けてエラーが出ないように書く  

```python
import heapq
from typing import List

class KthLargest:
    def __init__(self, k: int, nums: List[int]):
        self.k = k
        self.largest_k = []
        for num in nums:
            self.add(num)

    def add(self, val: int) -> int:
        heapq.heappush(self.largest_k, val)
        while len(self.largest_k) > self.k:
            heapq.heappop(self.largest_k)
        return self.largest_k[0]
```
