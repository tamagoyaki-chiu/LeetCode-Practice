最初のチャレンジ、よくわからないので、何もかけない。

一度、解答見ました
refer to https://github.com/fuga-98/arai60/pull/2

##解答##

```Python
#####################
class Solution:
    def hasCycle(self, head: Optional[ListNode]) -> bool:
        visited = set()
        node = head
        while node:
            if node in visited:
                return True
            visited.add(node)
            node = node.next
        return False
#####################
```

#####################
class Solution:
    def hasCycle(self, head: Optional[ListNode]) -> bool:
        start = hasCycle(head)
        reply = head
            if 
                start = head
                    return ture
            head = head.next

            
            return false
        break

#####################
