# 進め方

Step1 : 問題を解く。

Step2 : 他の人のPRを参照する。

Step3 : 3回続けてエラーが出ないように書く。ドキュメントを参照する。

## step 1

発想
1. for ループを作成, heapq.heappush を用いて nums をヒープ構造として管理し、  
   常に nums[0] が最小値になるようにする。  
2. nums[0] が target より小さい場合のみ処理を続行する、  
   インデックス index を 1 から nums の長さ - 1 までに設定する、  
   nums[0] と nums[index] を順番に比較する。  
```
もし nums[0] + nums[index] == target の場合：
その2つの値を結果として取り出す
nums から該当要素を削除する
結果を返す（または記録する）
```
```
もし nums[0] + nums[index] != target の場合：
index を 1 進めて次の要素を確認する
index が最後まで到達しても条件を満たす組み合わせが見つからない場合：
nums[0] を削除する
```
上記の処理を、組み合わせが見つからなくなるまで繰り返す
```python

```

## step 2

### 解きなおす

```python

```

### 他の方の PR を見てみる  
refer to  
```python

```

## step 3  
3回続けてエラーが出ないように書く  

```python

```
