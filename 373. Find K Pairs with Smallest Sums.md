# 進め方

Step1 : 問題を解く。

Step2 : 他の人のPRを参照する。

Step3 : 3回続けてエラーが出ないように書く。ドキュメントを参照する。

## step 1
まず 2 つの List の組み合わせ数（List1 の長さ × List2 の長さ）を計算し、  
それが k を超える場合は、両方の List を昇順に並べたうえで、  
大きい値を持つ側の末尾要素を削除していく方法を考えました。  

しかし、この方法には問題がありました。  
例えば k = 3 の場合、List1 と List2 がそれぞれ 2 要素ずつになった時点で、  
単純に末尾の大きい値を削除してしまうと、本来含まれるべき組み合わせまで除外してしまう可能性があります。  

つまり、「要素単位で削除すること」と「組み合わせの総和が小さい順に残すこと」は必ずしも一致せず、  
このアプローチには欠陥があることに実装中に気付きました。  
結局、解決策を見つける前に時間切れになってしまいました。

### 諦めたコード
```python
import heapq
from typing import List
from collections import Counter

class Solution:
    def kSmallestPairs(self, nums1: List[int], nums2: List[int], k: int) -> List[List[int]]:
        count = Counter(nums1 * nums2)
        heap = []

        for num1, num2 in count.items():
            heapq1.heappush(heap, nums1)
            heapq2.heappush(heap, nums2)

            if len(heapq1) * len(heapq2) > k and heapq[nums1[-1].val] >= heapq2[-1]:
                heappop(heapq1[-1])
            else if k > len(heapq1) * len(heapq2) and heapq1[-1] < heapq2[-1]:
                heappop(heapq2[-1])
            continue
        return [nums1 in heapq1,nums2 in heapq2]
```

## step 2

### 解きなおす
ChatGPT に自分のコードと発想を渡して修正を依頼した
```python
import heapq
from typing import List

class Solution:
    def kSmallestPairs(self, nums1: List[int], nums2: List[int], k: int) -> List[List[int]]:
        heap = []

        for n1 in nums1:
            for n2 in nums2:
                pair_sum = n1 + n2

                # マイナス数使って、模擬 max heap
                heapq.heappush(heap, (-pair_sum, n1, n2))

                if len(heap) > k:
                    heapq.heappop(heap)
            _, n1, n2 = heapq.heappop(heap)
            result.append([n1, n2])

        result.reverse()

        return result

        result = []

        while heap:
# O(m × n × log k)
```
最初は、nums1 と nums2 の末尾にある大きい要素を削除して組み合わせ数を減らす方法を考えた。しかし、この方法は正しくない。  
```python
例：
nums1 = [1, 2]
nums2 = [1, 100]
k = 2
```
作成される pair は以下の通り。
```python
(1,1) = 2
(2,1) = 3
(1,100) = 101
(2,100) = 102
```
答えは以下である。
```
(1,1)
(2,1)
```
この例では nums2 の 100 を削除しても偶然正しい結果になるが、常に正しいとは限らない。  
重要なのは、問題が求めているのは「小さい数値を残すこと」ではなく、「sum が小さい pair を残すこと」である。  
つまり、"要素単位で考える"じゃなくで、"pair 単位で考える"必要があります。  
要素が大きいからといって、その要素を含む pair が必ず不要になるわけではない。  

そのため、nums1 や nums2 の要素を直接削除する方法ではなく、  
```
(sum, num1, num2)
```
という pair 自体を heap で管理する必要がある。  

### 他の方の PR を見てみる  
refer to  
https://github.com/MA-yo-TA/leetcode/pull/11/changes  
https://github.com/hiro111208/leetcode/pull/10/changes  
```python
from typing import List
import heapq


class Solution:
    def kSmallestPairs(
        self, nums1: List[int], nums2: List[int], k: int
    ) -> List[List[int]]:
        candidates = []
        for index1 in range(min(k, len(nums1))):
            heapq.heappush(candidates, (nums1[index1] + nums2[0], (index1, 0)))

        k_smallest_pairs = []
        while len(k_smallest_pairs) < k:
            _, (index1, index2) = heapq.heappop(candidates)
            k_smallest_pairs.append([nums1[index1], nums2[index2]])

            index2 += 1
            if index2 < len(nums2):
                heapq.heappush(
                    candidates,
                    (nums1[index1] + nums2[index2], (index1, index2)),
                )

        return k_smallest_pairs
```
#### 処理の流れ
-> nums1 と nums2 の組み合わせを行列（Matrix）として考える。各 Row は左から右へ昇順になっていることを利用する。  

1.初期化
```
nums1 の各 Row の 先頭（col = 0） を Heap に追加する。
Heap に保存する内容は
sum
(row, col)（Pair の座標）
各 Row はこの時点で Heap に登録済みなので、後から row + 1 をする必要はない。
```
2. 最小値を取得
```
Heap の先頭は常に現在の最小 Pair。
heappop() で取り出す。
取り出した Pair を答え (k_smallest_pairs) に追加する。
```
3. 次の候補を追加
```
取り出した Pair の Row はそのまま。
Col だけ +1 して右隣を次の候補にする。
右隣が存在する場合だけ Heap に追加する。
```
4. 繰り返し
```
「最小 Pair を取り出す → 答えに追加 → 同じ Row の次を追加」
この流れを k 回 繰り返す。
```

refer to  https://github.com/shintaro1993/arai60/pull/14/changes  
```python
class Solution:
    def kSmallestPairs(self, nums1: List[int], nums2: List[int], k: int) -> List[List[int]]:
        pairs = [list(item) for item in product(nums1, nums2)]
        return sorted(pairs, key=sum)[:k]
```
#### 処理の流れ
1. product(nums1, nums2) を使って、すべての Pair を生成する。
2. 生成した Pair をリストに保存する。
3. sorted(..., key=sum) で Pair の合計値を基準に昇順ソートする。
4. ソート後、先頭から k 個 を返す。

## step 3  
3回続けてエラーが出ないように書く  

```python

```
